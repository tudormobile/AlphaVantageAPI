mode: ContinuousDeployment
workflow: GitHubFlow/v1
branches:
  main:
    increment: Patch
  feature:
    increment: Minor
  release:
    regex: ^releases?[\/-](?<version>\d+\.\d+\.\d+)$
    tag: 'rc'
    increment: None
